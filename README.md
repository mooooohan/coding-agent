# Qwen-Powered Real-Data Code Agent

一个基于阿里云 Qwen 大模型的智能代码生成代理系统，能够自动完成从项目规划、代码生成到质量检查的完整软件开发流程。

## 🎯 项目特点

- **多智能体协作**：包含 Planner（项目规划）、Coder（代码生成）、Reviewer（质量审查）三个专业智能体
- **真实数据集成**：内置 arXiv API 工具，可以获取真实的计算机科学论文数据
- **自主工具调用**：智能体能够根据任务需要自动调用合适的工具
- **质量保证机制**：每个任务完成后都会经过 Reviewer 的严格检查
- **实时上下文管理**：智能体在执行任务过程中会保持对当前工作区状态的感知

## 📁 项目结构

```
coding-agent/
├── agent.py            # Agent 类定义
├── main.py             # 系统主入口
├── config.py           # 配置文件（API Key、工作目录等）
├── tools.py            # 工具函数定义
├── prompts.py          # 智能体提示词
├── utils.py            # 通用工具函数
├── logger.py           # 日志工具
├── papers.json         # 论文数据存储
├── workspace/          # 默认工作区目录（生成的项目代码）
└── README.md           # 项目说明文档
```

## 🛠️ 安装与运行

### 环境要求
- Python 3.7+
- pip 包管理工具

### 安装步骤

1. **克隆或下载项目**
   ```bash
   git clone https://github.com/mooooohan/coding-agent.git
   cd coding-agent
   ```

2. **安装依赖包**
   ```bash
   pip install -r requirements.txt
   ```

3. **配置 API Key**
   编辑 `config.py` 文件，将你的阿里云 Qwen API Key 填入：
   ```python
   API_KEY = "your-api-key-here"
   ```

4. **运行系统**
   ```bash
   python main.py
   ```

## 🎮 使用说明

1. **系统启动**：运行 `main.py` 后，系统会显示欢迎信息并初始化三个智能体

2. **任务描述**：在 `main.py` 中修改 `task_description` 变量，定义你想要完成的软件开发任务

3. **自动执行**：系统会自动进入规划-执行-审查循环，直到所有任务完成

4. **结果查看**：生成的项目代码会保存在 `workspace` 目录下

5. **访问生成的网页**：
   - 进入生成的项目目录：`cd workspace`
   - 安装项目依赖：`pip install -r requirements.txt`
   - 启动网页应用：`python run.py`
   - 在浏览器中访问：`http://127.0.0.1:5000`

   *注意：不同项目可能有不同的启动方式，请参考生成项目中的 README.md 文件*

## 📝 示例任务

系统默认演示的任务是构建一个 'arXiv CS Daily' 网页应用：

```python
task_description = """
Build an 'arXiv CS Daily' webpage using Python Flask.
Requirements:
1. A script (e.g., data_fetcher.py) that uses the 'fetch_arxiv_papers' tool to get REAL papers for categories: cs.AI, cs.CV, cs.SE.
2. Save the fetched data into a JSON file (e.g., papers.json).
3. A Flask app (app.py) that reads papers.json and displays them.
4. The Homepage should list papers with titles and dates.
5. A Detail page showing the abstract/summary and link.
6. Navigation menu to filter by category.
"""
```

## 🔧 扩展功能

### 添加新工具
在 `tools.py` 中定义新的工具函数，并添加到 `AVAILABLE_TOOLS` 字典中

### 自定义智能体
在 `prompts.py` 中修改智能体的提示词，以调整其行为和专业性

### 调整配置参数
在 `config.py` 中修改模型名称、工作目录、温度参数等

## 📄 许可证

MIT License - 详见 LICENSE 文件

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请通过以下方式联系：
- Email: u3665787@connect.hku.hk
- GitHub: https://github.com/mooooohan
